Bot.py

Python library for making bots that use mouse and keyboard, and interact with display.

See bejeweled.py for example.

Windows and mac support untested but should work except for keyboard support.

Linux depencies:
    PIL
    xlib
    gtk
    pyatspi
